import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'back button android',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
